import type { MarketingPackage } from "@/lib/types";
export function MarketingPackageView({ marketing, locked }: { marketing: MarketingPackage; locked: boolean }) { return <section className={`success-panel ${locked ? "success-locked" : ""}`}><header><div><p className="success-kicker">Marketing package</p><h2>Book-specific content, ready for author review</h2></div><span className="asset-count">180 posts + 3 long-form assets</span></header><div className="channel-grid">{[["LinkedIn", marketing.linkedInPosts], ["Facebook", marketing.facebookPosts], ["X", marketing.xPosts], ["Threads", marketing.threadsPosts], ["Instagram", marketing.instagramCaptions], ["TikTok", marketing.tikTokVideoIdeas], ["YouTube Shorts", marketing.youtubeShortsIdeas]].map(([name, posts]) => <article key={name as string}><strong>{name as string}</strong><span>{(posts as string[]).length} assets</span><p>{(posts as string[])[0]}</p></article>)}</div>{locked && <div className="lock-overlay"><strong>Full marketing package locked in preview</strong><p>This advanced author-success asset is part of paid plan activation. Stripe activation is coming soon.</p></div>}</section>; }

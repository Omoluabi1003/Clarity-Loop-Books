import type { MarketabilityReport as Report } from "@/lib/types";
const labels: [keyof Report, string][] = [["marketabilityScore", "Marketability"], ["audienceClarityScore", "Audience clarity"], ["titleStrengthScore", "Title strength"], ["subtitleStrengthScore", "Subtitle strength"], ["coverStrengthScore", "Cover strength"], ["positioningScore", "Positioning"], ["readerPromiseScore", "Reader promise"], ["categoryFitScore", "Category fit"], ["salesReadinessScore", "Sales readiness"]];
export function MarketabilityReport({ report }: { report: Report }) { return <section className="success-panel"><header><div><p className="success-kicker">Marketability analyzer</p><h2>Launch signal, not a sales promise</h2></div><span className={`success-score ${report.marketabilityScore >= 75 ? "ready" : "review"}`}>{report.marketabilityScore}<small>/100</small></span></header><div className="score-grid">{labels.slice(1).map(([key, label]) => <div key={key}><span>{label}</span><strong>{report[key] as number}</strong><i><b style={{ width: `${report[key]}%` }} /></i></div>)}</div><div className="recommendation-grid"><div><h3>Top weaknesses</h3>{report.topWeaknesses.map((item) => <p key={item}>• {item}</p>)}</div><div><h3>Priority recommendations</h3>{report.topRecommendations.map((item) => <p key={item}>→ {item}</p>)}</div></div></section>; }

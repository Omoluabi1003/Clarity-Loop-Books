import type { MonetizationPackage } from "@/lib/types";
export function MonetizationPackageView({ monetization, locked }: { monetization: MonetizationPackage; locked: boolean }) { return <section className={`success-panel ${locked ? "success-locked" : ""}`}><header><div><p className="success-kicker">Monetization paths</p><h2>Extend the book without forcing the wrong business model</h2></div></header><div className="asset-grid"><article><small>WORKSHOP</small>{monetization.workshopOutline.map((x) => <p key={x}>• {x}</p>)}</article><article><small>KEYNOTE TOPICS</small>{monetization.keynoteTopics.map((x) => <p key={x}>• {x}</p>)}</article><article><small>LEAD MAGNETS</small>{monetization.leadMagnetIdeas.map((x) => <p key={x}>• {x}</p>)}</article><article><small>OFFER DIRECTION</small><p>{monetization.consultingOffer}</p></article></div>{locked && <div className="lock-overlay"><strong>Studio access preview</strong><p>Monetization export unlocks with Studio activation. No payment or Stripe dependency is required during beta.</p></div>}</section>; }

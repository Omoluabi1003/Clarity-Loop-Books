# Optional for the current deterministic studio demo.
# Add a provider key when connecting production AI generation.
OPENAI_API_KEY=

# Future hosted-service integration (not required for the browser-local beta)
DATABASE_URL=
